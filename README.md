# HTML Studio (Yarub HTML Playground)

Main editor: **`index.html`** (single-page app).

**Mobile builds (Android APK + iOS Xcode project via GitHub Actions):** see the guide **`mobile-sync-backend/MANUAL.md`** and service **`mobile-sync-backend/README.md`**.
